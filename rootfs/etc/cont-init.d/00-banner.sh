#!/usr/bin/env bash
# Print the shared Junker-der-Provinz init-log banner once at startup,
# before the rest of init runs (lexicographic order: 00 before 10).
bash /usr/local/bin/print-banner.sh "Matrix" "Synapse + Element + coturn, plug-and-play for Unraid"
